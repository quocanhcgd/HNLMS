import {describe,expect,it} from "vitest";import {TenantPlatformService,TenantIsolationError} from "../../src/modules/tenant-platform/tenant-platform.service.js";describe("Phase20 tenant migration",()=>{it("keeps tenants isolated and performs one-way cutover",()=>{const s=new TenantPlatformService([{id:"t1",organizationId:"o1",databaseName:"db1",endpoint:"saas",mode:"saas",provisioning:"pending",migration:"idle",readOnly:false,quota:100,schemaVersion:"1"},{id:"t2",organizationId:"o2",databaseName:"db2",endpoint:"saas",mode:"saas",provisioning:"ready",migration:"idle",readOnly:false,quota:100,schemaVersion:"1"}]);s.provision("o1");expect(s.preflight("o1","checksum").ready).toBe(true);expect(s.cutover("o1","checksum").mode).toBe("dedicated");expect(()=>s.get("o3")).toThrow(new TenantIsolationError("tenant_not_found"));expect(()=>s.assertQuota("o1",101)).toThrow("quota_exceeded")})});
