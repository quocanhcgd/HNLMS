import {describe,expect,it} from "vitest";import {LessonDocumentSchema} from "@hnlms/domain-contracts";import {MediaUploadService} from "../../src/shared/storage/media-upload.service.js";import {createPlaybackToken,verifyPlaybackToken} from "../../src/shared/storage/playback.service.js";describe("Phase22 multimedia foundation",()=>{it("validates lesson document and idempotent upload checksum",()=>{expect(LessonDocumentSchema.parse({schemaVersion:1,title:"Lesson",blocks:[{id:"b1",type:"text",data:{text:"Hi"}}]}).title).toBe("Lesson");const s=new MediaUploadService();const x=s.create({organizationId:"o1",storageKey:"private/a",mimeType:"image/png",checksum:"c1"});expect(s.complete(x.id,"c1").status).toBe("processing")});it("expires playback token",()=>{const t=createPlaybackToken({organizationId:"o",assetId:"a",scope:"student",expiresAt:new Date("2099-01-01")});expect(verifyPlaybackToken(t).assetId).toBe("a");expect(()=>verifyPlaybackToken(t,new Date("2100-01-01"))).toThrow("playback_token_expired")})});
