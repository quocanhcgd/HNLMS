import {describe,expect,it} from "vitest";import {AiGateway,AiPolicyError,AiReviewService} from "../../src/modules/ai-governance/ai-gateway.service.js";describe("US14 AI governance",()=>{const actor={userId:"u1",organizationId:"o1",scopes:["content"]};it("allows scoped task and blocks out-of-scope/high-risk",async()=>{const g=new AiGateway();expect((await g.run(actor,{taskType:"summary",scope:"content",input:{context:"lesson"},risk:"low"})).status).toBe("completed");await expect(g.run(actor,{taskType:"x",scope:"payroll",input:{},risk:"low"})).rejects.toMatchObject({code:"scope_denied"});expect((await g.run(actor,{taskType:"recommend",scope:"recommendation",input:{},risk:"high"})).status).toBe("manual_review")});it("supports approval and appeal",()=>{const r=new AiReviewService();expect(r.approve({status:"manual_review",reviewerId:"reviewer"}).status).toBe("approved");expect(r.appeal("Không đồng ý").status).toBe("appealed")})});
