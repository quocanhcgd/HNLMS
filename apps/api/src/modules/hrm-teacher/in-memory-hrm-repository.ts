import type { Assignment } from "./hrm-teacher.service";
export class InMemoryHrmRepository{employees:any[]=[];leaves:any[]=[];assignments:Assignment[]=[];async findEmployee(x:any){return this.employees.find(e=>e.organizationId===x.organizationId&&e.id===x.employeeId)||null}async updateEmployee(e:any){const i=this.employees.findIndex(x=>x.id===e.id);this.employees[i]=e;return e}async saveLeave(l:any){this.leaves.push(l);return l}async saveAssignment(a:Assignment){const i=this.assignments.findIndex(x=>x.id===a.id);if(i<0)this.assignments.push(a);else this.assignments[i]=a;return a}}
