import { index, integer, jsonb, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";import { organizations, users } from "../../database/schema/identity-access";export const mediaAssetStatus=pgEnum("media_asset_status",["created","uploading","scanning","processing","ready","failed","quarantined"]);export const mediaAssets=pgTable("multimedia_assets",{id:uuid("id").defaultRandom().primaryKey(),organizationId:uuid("organization_id").notNull().references(()=>organizations.id),storageKey:text("storage_key").notNull(),mimeType:text("mime_type").notNull(),checksum:text("checksum").notNull(),sizeBytes:integer("size_bytes").notNull(),status:mediaAssetStatus("status").notNull().default("created"),metadata:jsonb("metadata"),createdByUserId:uuid("created_by_user_id").notNull().references(()=>users.id),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp("updated_at",{withTimezone:true}).notNull().defaultNow()},t=>({storageUnique:uniqueIndex("multimedia_assets_org_storage_unique").on(t.organizationId,t.storageKey),checksumIndex:index("multimedia_assets_org_checksum_index").on(t.organizationId,t.checksum)}));export const mediaDerivatives=pgTable("multimedia_derivatives",{id:uuid("id").defaultRandom().primaryKey(),assetId:uuid("asset_id").notNull().references(()=>mediaAssets.id),kind:text("kind").notNull(),storageKey:text("storage_key").notNull(),mimeType:text("mime_type").notNull(),metadata:jsonb("metadata"),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow()},t=>({assetKindUnique:uniqueIndex("multimedia_derivatives_asset_kind_unique").on(t.assetId,t.kind)}));
