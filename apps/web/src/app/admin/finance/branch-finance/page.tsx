"use client";import {PageHeader} from "@/components/app-shell";import {Group,Paper,Text} from "@mantine/core";import {UiDataTable,UiStatusBadge,UiTextInput} from "@/components/ui";import {Search} from "lucide-react";import {useMemo,useState} from "react";import type {ColumnDef,SortingState} from "@tanstack/react-table";import {getCoreRowModel,getFilteredRowModel,getSortedRowModel,useReactTable} from "@tanstack/react-table";type Row={branch:string;income:string;expense:string;balance:string;status:"Đang theo dõi"|"Cần đối soát"};const rows:Row[]=[{branch:"Hà Nội",income:"185.000.000đ",expense:"82.000.000đ",balance:"103.000.000đ",status:"Đang theo dõi"},{branch:"Cầu Giấy",income:"96.000.000đ",expense:"64.000.000đ",balance:"32.000.000đ",status:"Cần đối soát"}];export default function BranchFinancePage(){const[q,setQ]=useState("");const[sort,setSort]=useState<SortingState>([]);const data=useMemo(()=>rows.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())),[q]);const columns=useMemo<ColumnDef<Row>[]>(()=>[{accessorKey:"branch",header:"Chi nhánh"},{accessorKey:"income",header:"Thu"},{accessorKey:"expense",header:"Chi"},{accessorKey:"balance",header:"Số dư"},{accessorKey:"status",header:"Trạng thái",cell:({getValue})=><UiStatusBadge role={getValue<Row["status"]>()==="Cần đối soát"?"warning":"success"}>{getValue<string>()}</UiStatusBadge>}],[]);const table=useReactTable({data,columns,state:{sorting:sort,globalFilter:q},onSortingChange:setSort,onGlobalFilterChange:setQ,getCoreRowModel:getCoreRowModel(),getFilteredRowModel:getFilteredRowModel(),getSortedRowModel:getSortedRowModel()});return <div className="page"><PageHeader title="Tài chính chi nhánh" subtitle="Theo dõi ngân sách, thu chi, số dư và đối soát theo từng chi nhánh."/><Group mb="md"><UiTextInput value={q} onChange={e=>setQ(e.currentTarget.value)} leftSection={<Search size={16}/>} placeholder="Tìm chi nhánh..."/></Group><Paper className="panel" p="md" withBorder><UiDataTable table={table} columnCount={columns.length} minWidth={760} emptyTitle="Không tìm thấy chi nhánh."/></Paper></div>}
