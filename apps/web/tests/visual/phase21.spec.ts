import {expect,test} from "@playwright/test";test("Phase21 reporting layout renders across desktop and mobile",async({page})=>{await page.setViewportSize({width:1440,height:900});await page.goto("/admin/reporting");await expect(page.getByText("Báo cáo vận hành",{exact:true})).toBeVisible();await page.setViewportSize({width:390,height:844});await page.reload();await expect(page.getByText("Báo cáo vận hành",{exact:true})).toBeVisible()});
