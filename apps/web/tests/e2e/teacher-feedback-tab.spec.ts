import {expect,test} from "@playwright/test";test("teacher submits class feedback to academic affairs",async({page})=>{await page.goto("/teacher/classes/if-2609/feedback");await expect(page.getByText("Ghi nhận phản hồi buổi học",{exact:true})).toBeVisible();await page.getByLabel("Mức độ ưu tiên").click();await page.getByRole("option",{name:"Cao"}).click();await page.getByLabel("Nội dung phản hồi").fill("Cần thêm thời gian speaking cho buổi sau.");await page.getByRole("button",{name:"Gửi học vụ"}).click();await expect(page.getByText("Đã gửi học vụ",{exact:true})).toBeVisible();});
