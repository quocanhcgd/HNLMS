import { expect, test } from "@playwright/test";
test.describe("US11 finance",()=>{test("admin sees finance invoice table",async({page})=>{await page.goto("/admin/finance");await expect(page.locator(".pageHeader").getByText("Tài chính",{exact:true})).toBeVisible({timeout:15000});await expect(page.getByText("INV-2608-001",{exact:true})).toBeVisible()});test("student and parent see payment action",async({page})=>{await page.goto("/student/billing");await expect(page.getByRole("button",{name:"Thanh toán"})).toBeVisible();await page.goto("/parent/payments");await expect(page.getByRole("button",{name:"Thanh toán"})).toBeVisible()})});
