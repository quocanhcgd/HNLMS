import { expect,test } from "@playwright/test";test.describe("US12 HRM",()=>{test("admin HRM and teacher portal",async({page})=>{await page.goto("/admin/hrm");await expect(page.locator(".pageHeader").getByText("Nhân sự & giáo viên",{exact:true})).toBeVisible({timeout:15000});await expect(page.getByText("GV-001 · Lan Anh",{exact:true})).toBeVisible();await page.goto("/teacher");await expect(page.locator(".pageHeader").getByText("Tổng quan giảng dạy",{exact:true})).toBeVisible()})});
