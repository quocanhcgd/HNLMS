import { expect, test } from "@playwright/test";
test("assignment tab supports filtering creation and grading workflow",async({page})=>{await page.goto("/teacher/classes/if-2609/assignments");await page.getByLabel("Lọc loại bài").click();await page.getByRole("option",{name:"Bài tập về nhà"}).click();await expect(page.getByText("Writing Task 1",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Tạo bài"}).click();await expect(page.getByText("Tạo bài tập cho lớp",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Lưu bản nháp"}).click();await expect(page.getByText("Đã tạo bài tập nháp cho lớp.",{exact:true})).toBeVisible();await page.getByText("Writing Task 1",{exact:true}).click();await expect(page.getByText("Hàng đợi xử lý bài tập",{exact:true})).toBeVisible();await expect(page.getByRole("button",{name:"Chấm bài"})).toBeVisible();});
