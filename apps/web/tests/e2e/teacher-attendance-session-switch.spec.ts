import { expect, test } from "@playwright/test";test("attendance table changes with selected session",async({page})=>{await page.goto("/teacher/classes/if-2609/attendance");await expect(page.getByText("Điểm danh: Buổi 5 · Listening clinic",{exact:true})).toBeVisible();await page.getByLabel("Chọn buổi điểm danh").click();await page.getByRole("option",{name:"Buổi 4 · Listening warm-up"}).click();await expect(page.getByText("Điểm danh: Buổi 4 · Listening warm-up",{exact:true})).toBeVisible();await expect(page.getByRole("textbox", { name: "Trạng thái Phạm Minh Khoa" })).toHaveValue("Có phép");});
